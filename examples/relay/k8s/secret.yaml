# Example only - DO NOT commit real secrets.
# Generate with:
#   openssl rand -hex 32 | base64
# and replace the placeholder.
#
# In production, use an external secret manager (External Secrets
# Operator, Vault, SOPS) instead of committing a Secret object.
apiVersion: v1
kind: Secret
metadata:
  name: purple-wolf-relay-secrets
  namespace: purple-wolf
type: Opaque
data:
  # base64("REPLACE_ME")
  SIEM_HMAC_SECRET: UkVQTEFDRV9NRQ==
