displayName: purple-wolf
type: middleware
runtime: wasm
import: github.com/guaracloud-oss/purple-wolf
summary: |
  Hybrid Web Application Firewall: libinjection (SQLi/XSS), aho-corasick
  literal signatures, structural anomaly checks, per-IP rate limiting.

testData:
  mode: monitor
  groups:
    injection:
      enabled: true
      mode: enforce
